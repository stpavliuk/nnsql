plugins {
    id 'buildlogic.parser-library-conventions'
}

group = 'rai.sql.parser.sql'
version = '0.0.1'

generateGrammarSource {
    arguments += ['-package', 'parser.sql']
    outputDirectory = "${projectDir}/src/main/java/" as File
}
